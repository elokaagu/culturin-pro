/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lovable.dev"],
  },
  // Enable static exports if needed
  // output: 'export',
  // trailingSlash: true,
};

module.exports = nextConfig;
